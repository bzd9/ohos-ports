name: Update @ohos-ports Package List

on:
  schedule:
    # 每天北京时间凌晨 2:00（UTC 18:00）
    - cron: '0 18 * * *'
  workflow_dispatch: # 手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Update packages.json and README
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -ex
          export NPM_REGISTRY="https://registry.npmjs.org"
          if [ -n "$NPM_TOKEN" ]; then
            export NPM_AUTH_TOKEN="$NPM_TOKEN"
          fi
          python3 docs/update-packages.py

      - name: Commit and push if changed
        run: |
          set -ex
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/packages.json README.md
          if git diff --staged --quiet; then
            echo "No changes, skipping commit"
          else
            git commit -m "auto: update @ohos-ports package list [skip ci]"
            git push
          fi
